workflow:
  id: pr-workflow-1
  name: PR Workflow
  description: Spec -> prototype -> review -> ship
  i18n:
    zh-CN:
      description: 规格说明 → 原型开发 → 代码审查 → 发布上线


database:
  db_file: db/pr_workflow.db
  db_id: pr_workflow_db

nodes:
  - Spec
  - Prototype
  - Review
  - Ship

trigger: Manual launch

outputs:
  - Spec
  - UI prototype
  - QA summary
